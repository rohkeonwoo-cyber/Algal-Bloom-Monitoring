# 실시간 데이터 파이프라인 설정법

`scripts/fetch_hrfco.py` + `scripts/fetch_algae.py` 가 공공API를 수집해 `data/*.json` 을
갱신하고, 그 JSON을 GitHub 저장소에 커밋하면 페이지가 최신값을 읽습니다.

## 왜 "로컬 수집" 구조인가 (2026-09-11 변경)

처음에는 GitHub Actions가 매시간 수집하도록 만들었지만 **1차 실행이 44초만에 실패**했습니다.

```
TimeoutError: timed out
urllib.error.URLError: <urlopen error timed out>
  → scripts/fetch_hrfco.py, api_get("waterlevel/info.json")
```

401/403(키 문제)이 아니라 **TCP 연결 자체가 타임아웃** — GitHub Actions 실행기는 해외
데이터센터(Azure)에 있고 한강홍수통제소 API(`api.hrfco.go.kr`)는 그 대역에서 닿지 않습니다.
키를 다시 발급받아도 해결되지 않는 종류의 문제입니다.

그래서 **수집은 국내 IP(연구실 컴퓨터)에서 하고, 결과 JSON만 저장소에 올리는** 구조로
바꿨습니다. 페이지는 여전히 GitHub Pages 정적 호스팅이라 서버 비용이 없습니다.

```
[연구실 PC]  cron 매시 10분
   └ fetch_hrfco.py / fetch_algae.py  →  data/*.json
        └ git commit & push  →  [GitHub 저장소]  →  [GitHub Pages 페이지가 읽음]
```

## 검증 상태

**두 스크립트 모두 실제 키로 로컬 실행 검증 완료.** `data/` 안의 JSON은 실데이터입니다.
- HRFCO: 낙동강 유역 수위 255개소(위험등급 판정가능 191) / 강수 112개소 / 홍수예보발령 0건,
  소요 약 1분 43초
- 조류경보: 4개 지점 최신값 수집 (수집 시점 기준 칠서·물금매리 '경계', 해평 '관심')

### 실제 응답이 문서와 달라 고친 부분 (참고용 기록)
1. HRFCO 응답 필드명이 **소문자**(`wlobscd`, `obsnm`, `attwl`…) — 문서는 대문자 표기
2. 위경도가 **도-분-초 문자열**(`128-33-04`) — 십진도 변환 필요
3. 시계열이 **최신순(내림차순)** 응답 — 배열 끝을 최신으로 가정하면 30시간 전 값을 집게 됨
4. 조류경보 최신 기록은 값이 `'분석중'`일 수 있음 — 수치가 나온 최근 기록으로 등급 판정하고
   `pending` 플래그로 분석중 여부를 따로 표시
5. HRFCO 호출 제한("1분 1000회 초과 3번이면 차단")에 맞춰 요청 간격 0.1초

---

# A. 이 컴퓨터에 설치된 것 (2026-09-11 설치 완료)

| 항목 | 위치 | 상태 |
|---|---|---|
| 저장소 클론 | `~/Algal-Bloom-Monitoring` | 완료 |
| API 키 파일 | `~/.config/nakdong-keys.env` (권한 600) | 완료 |
| git 신원 | `user.name` / `user.email` 전역 설정 | 완료 |
| SSH 키 | `~/.ssh/id_ed25519` (비밀번호 없음) | **공개키 GitHub 등록 필요** |
| 실행 스크립트 | `~/Algal-Bloom-Monitoring/scripts/run_local_update.sh` | 완료 |
| 자동 실행 | crontab `10 * * * *` | 완료 |
| 로그 | `~/nakdong-update.log` | 실행 시 생성 |

키는 **저장소 밖**(`~/.config/`)에 둡니다. 저장소 안에 두면 GitHub에 공개되기 때문입니다.
`.gitignore` 에 `*.env`, `__pycache__/` 를 추가해 실수로 올라가는 것도 막았습니다.

## 남은 한 가지 — 공개키 등록 (사용자가 직접)

푸시 권한은 계정 소유자만 줄 수 있어서 이 단계만 직접 해주셔야 합니다.

1. https://github.com/settings/keys → **New SSH key**
2. Title: 아무거나 (예: `연구실 PC`)
3. Key: 아래 명령으로 출력되는 한 줄을 전체 복사해서 붙여넣기
   ```bash
   cat ~/.ssh/id_ed25519.pub
   ```
4. **Add SSH key**

등록 후 확인:
```bash
ssh -T git@github.com          # "Hi rohkeonwoo-cyber! You've successfully authenticated" 가 나오면 성공
bash ~/Algal-Bloom-Monitoring/scripts/run_local_update.sh    # 수집+푸시 한 번 수동 실행
```

## 수동으로 지금 갱신하고 싶을 때
```bash
bash ~/Algal-Bloom-Monitoring/scripts/run_local_update.sh
```

## 잘 돌고 있는지 확인
```bash
tail -40 ~/nakdong-update.log                    # 최근 실행 로그
cd ~/Algal-Bloom-Monitoring && git log --oneline -5   # 커밋이 매시간 쌓이는지
```

## 주의: PC가 꺼져 있으면 갱신되지 않음
cron은 이 컴퓨터가 켜져 있을 때만 돕니다. 꺼진 동안은 페이지가 마지막 수집값을 그대로
보여줍니다(`updated_at` 필드에 수집 시각이 박혀 있으니 언제 값인지 확인 가능).
항상 갱신이 필요하면 국내 서버(예: 학교 서버, 국내 클라우드)에 같은 cron을 걸면 됩니다.

---

# B. GitHub Actions (보조 — 되면 좋고 안 되면 무해)

`.github/workflows/update-data.yml` 은 폐기하지 않고 **진단용으로 남겨뒀습니다**.
- `접속 진단` 단계에서 `api.hrfco.go.kr` 과 `apis.data.go.kr` 에 각각 curl을 쏴
  어느 쪽이 막히는지 로그로 남깁니다.
- 두 수집 단계에 `continue-on-error: true` 를 걸어, 한쪽이 막혀도 다른 쪽은 진행합니다.
- 커밋 단계는 변경이 없으면 건너뜁니다(빈 커밋 실패 방지).

조류경보제(`apis.data.go.kr`)는 해외에서도 닿을 가능성이 있어서, Actions 탭에서 수동 1회
실행(**Run workflow**)해보면 확실해집니다. 만약 닿는다면 "조류경보는 Actions가, 수위·강수는
로컬이" 맡는 하이브리드로 갈 수 있습니다. 안 닿아도 로컬 수집이 전부 커버하므로 문제없습니다.

Secrets는 이미 등록해두셨습니다(`HRFCO_KEY`, `DATA_GO_KR_KEY`).
